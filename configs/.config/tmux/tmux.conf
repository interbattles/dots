set-option -sa terminal-overrides ",foot*:Tc"

unbind C-b
set-option -g prefix C-a

set -g mouse on

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

source ~/.cache/matugen/tmux.conf
run '~/.config/tmux/plugins/tpm/tpm'
