(deflisten battery
  :initial false
  `fish functions/battery`)

(deflisten weather 
  :initial ''
  `fish functions/weather`)

(deflisten volume
  :initial 0
  `fish functions/volume listen`)

(deflisten brightness
  :initial 0
  `fish functions/brightness listen`)

(deflisten workspaces
  `fish functions/workspaces listen`)

;(deflisten notification
;  `fish functions/notification listen`)
